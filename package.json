{
    "name": "factorio-must-fall-tooling",
    "private": true,
    "description": "Dev tooling only — NOT shipped in the mod. The mod itself is pure Lua.",
    "scripts": {
        "defs": "fmtk luals-addon -o 2.0.76 dev/factorio-defs",
        "flib": "rm -rf dev/flib dev/.flib-tmp && mkdir -p dev/flib && unzip -q -o \"$HOME/Library/Application Support/factorio/mods/\"flib_*.zip -d dev/.flib-tmp && mv dev/.flib-tmp/*/* dev/flib/ && rm -rf dev/.flib-tmp",
        "lint": "luacheck .",
        "package": "fmtk package",
        "datestamp": "fmtk datestamp",
        "version": "fmtk version",
        "publish": "fmtk publish"
    },
    "devDependencies": {
        "factoriomod-debug": "^2.0.14"
    }
}
